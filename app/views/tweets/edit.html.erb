<div class="contents row">
  <div class="container">
    <%= form_with(model: @tweet, local: true) do |form| %>
      <h3>
        編集する
      </h3>

      <%= render partial: "form", locals: { form: form } %> <!--部分テンプレートの呼び出し-->

    <% end %>
  </div>
</div>